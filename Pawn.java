package chess;

import chess.Piece;

public class Pawn implements Piece {

    boolean alreadyMoved = 0;

    public boolean validMove(int startFile, int startRank, int endFile, int endRank, boolean occupied) {
        if (!alreadyMoved) {

            

        }
        return true;

    }

    public boolean move(int startFile, int startRank, int endFile, int endRank, boolean occupied) {
        if (!alreadyMoved) {

        }
        return true;
    }


}